{
  "name": "crm-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd server && node --watch index.js\" \"cd client && npx vite --port 3000\"",
    "seed": "cd server && node seed.js",
    "vercel-build": "cd client && npm install && npm run build",
    "install:all": "cd server && npm install && cd ../client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
